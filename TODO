* x86: fix return statments from if-else bodies
  test code:

int foo(int a)
{
  if (a < 0)
  {
    return -1;
  }
  else if (a > 0)
  {
    return 1;
  }
  else {
    return 0;
  }
}

The generated code for return above will get invalid dummy jump labels.
